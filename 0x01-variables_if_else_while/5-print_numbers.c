#include<stdlib.h>
#include<stdio.h>

/**
	*	main	- the function main use printf to print numbers
	*
	*	Return:	Always	0	(Success)
	*/
int	main(void)
{
	int i;

	for (i = 0; i < 10; ++i)
	{
		printf("%d", i);
	}
	printf("\n");
	return	(0);
}
